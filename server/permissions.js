JandresNotes.notes.allow({
    insert: function(user_id, note) {
        // TODO: Make the user register a callback.
        return true;
    }
});

JandresNotes.boards.allow({
    insert: function(user_id, board) {
        // TODO: Make the user register a callback.
        return true;
    }
});